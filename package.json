{
  "name": "tsxgen",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "build": "ts-node --transpile-only build.ts && tsc",
    "test": "npm run build && node test.js"
  },
  "dependencies": {
    "typescript": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "ts-node": "^10.7.0"
  }
}