{
  "scripts": {
    "build": "ts-node --transpile-only build/args && ts-node --transpile-only build/index",
    "test": "ts-node --transpile-only test.tsx"
  },
  "dependencies": {
    "typescript": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.21"
  }
}