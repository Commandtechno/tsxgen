{
  "name": "tsxgen",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "ts-node --transpile-only build.ts",
    "test": "tsc && node test"
  },
  "dependencies": {
    "typescript": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "ts-node": "^10.7.0"
  }
}